const Genres = ({genrenames}) => {
    return (
        <>
            <div className="text-color-primary border-color-blueform rounded-lg">
                <h4>{genrenames}</h4>
            </div>
        </>
    );
}